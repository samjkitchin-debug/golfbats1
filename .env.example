NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Migration token (dev only, for one-off passport data migration)
# Generate a secure random string, e.g: openssl rand -hex 32
# MIGRATION_TOKEN=
